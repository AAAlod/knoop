# Changelog

## 0.1.10-alpha

- 明确 AI 生成题的题型、选项和答案 JSON 格式，减少题目校验失败。
- 自动修正常见且无歧义的题型别名与单元素答案格式；其余不合规结果显示具体题号和字段，仍须通过题库校验才可保存。
- 未通过校验的批次保留为失败状态；可手动重试，但重试会再次请求 AI API，可能产生费用。

## 0.1.9-alpha

- 修复 AI 候选题通过网络请求后，题库校验器在 Android 前端抛出“is not a function”，导致单题和批量回炉无法完成。
- 增加 Vite 产物回归测试，覆盖生成结果的 JSON Schema 校验。

## 0.1.8-alpha

- 修复 Android 点击 AI 生成后可能直接退出：DeepSeek HTTPS 请求改用显式 Rustls 配置和内置受信任根证书，避免未初始化的 Android 系统证书验证器。
- Release 网络任务发生 Rust panic 时返回错误提示，避免整个应用进程退出。

## 0.1.7-alpha

- 将当前错题页操作精简为“导出”“批量回炉”“练习”。
- AI 回炉预置 DeepSeek API 地址与 V4.1 Flash 模型；只需填写 API Key。
- API Key 保存在本机应用私有数据目录，重启后自动恢复；可在配置中更换或清除。

## 0.1.6-alpha

- 当前错题支持批量 AI 回炉：展示预计候选题数和批次数，确认后按批顺序生成，每次最多请求 12 道原题。
- 每批最多包含 2 道原题；通过校验的结果和任务进度保存在本机。失败批次可单独重试，已成功的原题无需重复请求。
- 支持取消、退出后继续、候选题核对，以及将完整通过校验的批次保存为独立临时题组并立即刷题。
- 校验跨批次题目 ID、内容重复及原题来源，临时练习仍与正式题库和作答记录隔离。

## 0.1.5-alpha

- AI 候选变式题经人工核对后可保存为独立临时题组并立即练习。
- 临时题组保留原题派生关系、生成时间和模型信息；练习进度可在应用重启后继续，也可从头开始或删除。
- 临时作答只写入临时题组，不改变正式题库、作答记录、当前错题或收藏。
- 与正式刷题共用单选、多选和填空判分规则。

## 0.1.4-alpha

- 当前错题支持单题 AI 回炉，配置 OpenAI-compatible Chat Completions 接口后可生成两道候选变式题并预览。
- AI 请求在 Rust 层执行，支持超时和取消；API Key 仅在本次应用运行期间保存在内存。
- AI 输出经过题库 JSON Schema、语义和回炉专属约束校验；候选题不会写入正式题库或作答记录。
- 修复 Android 启动后仅显示背景：题库 Schema 校验器改为构建时生成，运行时不再执行动态代码。

## 0.1.3-alpha

- 将范围选择的全部展开与折叠操作统一移至右上角。
- 精简错题详情：绿色选项直接表示正确，多选错题分别显示错选与漏选，不重复列出正确答案。
- 统一答题结果、表单操作、空状态、反馈提示、题型标签和危险操作的界面表达。

## 0.1.2-alpha

- 快刷模式下，多选题改为手动提交答案。
- 统一折叠箭头的形状和旋转中心，移除折叠按钮的可见方框。
- 答题页直接显示收藏、斩杀、审核、笔记和编辑操作。

## 0.1.1-alpha

- 改善首页、组题、目录和答题界面的信息层级与操作体验。
- 提升列表可访问性、触控区域和学习内容的可读性。

## 0.1.0-alpha

Initial public alpha.
